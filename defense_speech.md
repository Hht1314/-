# 课堂行为分析与人脸识别系统 — 答辩演讲稿

---

## 第一部分：系统介绍（约 6-8 分钟）

---

### 开场（约 30 秒）

各位老师好！我是**胡海涛**，学号 **202231418**，毕业设计题目是《基于图像识别技术的多维学情剖析系统的设计与实现》，指导老师是**孙浩宸**老师。

本系统是一个集姿态估计、行为识别、人脸识别于一体的**学情剖析平台**。接下来我从"为什么做 → 提供了哪些功能 → 技术怎么实现"三条线展开。

---

### 一、背景与目标（约 1 分钟）

传统课堂管理有三个痛点：
**人工点名效率低、学生状态难量化、教学改进缺依据**。本课题的目标是利用计算机视觉技术，让监控摄像头成为老师的"第二双眼睛"，自动化完成学生身份识别、课堂行为检测、数据分析和教学建议生成。

---

### 二、系统功能全览（约 3 分钟，重点）

系统提供了 **5 大功能模块**，覆盖从数据采集到分析输出的完整链路。

---

#### 功能 1：人脸注册录入工具

**核心文件**：`face_recognition/face_registration_tool.py`

在正式使用人脸识别之前，需要先把学生的人脸信息录入系统。我专门开发了一个命令行人脸注册工具，支持 **三种录入模式**：

- **摄像头实时录入**（`--mode camera`）：学生面对摄像头，按空格键采集多角度人脸样本（默认 5 张），系统自动提取 ArcFace 512 维特征向量并存入本地人脸库。采集过程中画面实时显示人脸检测框和采集进度。

- **单张图片录入**（`--mode image`）：给定一张学生照片，自动检测人脸并注册。

- **文件夹批量录入**（`--mode folder`）：给定一个照片文件夹，自动遍历所有图片，批量提取特征并注册，适合一次性导入全班学生照片。

另外还有 **查询模式**（`--mode list`）列出所有已注册人员，和 **删除模式**（`--mode delete`）删除指定人员。这套工具使得人脸库的建库和管理完全不需要写代码。

底层支撑文件：`face_detector.py`（ArcFace 检测与特征提取）、`face_database.py`（人脸库增删改查、特征向量 pkl + metadata.json 持久化）。

---

#### 功能 2：实时摄像头检测（行为 + 人脸同时运行）

**核心文件**：`behavior_classification/real_time_detection.py`

这是系统的核心演示功能。启动后，摄像头画面中会实时显示：

- **骨骼叠加**：YOLOv11-Pose 提取的 17 个关节点骨架画在每个人身上
- **行为标签**：当前识别到的课堂行为（如"举手"、"听讲"）显示在人像上方
- **人脸识别框**：检测到的人脸标注姓名（需提前在功能 1 中注册）
- **FPS 帧率**：左上角实时显示处理帧率

技术实现上，采用 **50 帧滑动窗口** 缓存骨架序列，每累积满 50 帧就调用 ST-GCN 做一次行为分类预测。为了防止单帧误检导致行为标签来回跳变，加入了 **预测平滑机制**：缓存最近 5 帧的预测结果，通过多数投票 + 置信度加权决定最终输出标签。

运行方式：`python behavior_classification/real_time_detection.py`

---

#### 功能 3：视频文件人脸识别

**核心文件**：`face_recognition/video_face_recognition.py`

除了摄像头实时检测外，系统也支持对已有视频文件进行独立的人脸识别分析。输入一段课堂视频，逐帧检测人脸、提取 512 维特征、与本地人脸库做余弦相似度匹配，输出每帧的身份识别结果。适合单独做签到验证的场景。

运行方式：`python face_recognition/video_face_recognition.py`

---

#### 功能 4：Web 管理平台（上传 → 异步处理 → 数据面板 → AI 建议）

这是系统的 **完整交付形态**，一个基于 Django 的 Web 应用。核心文件包括：

| 文件 | 作用 |
|---|---|
| `web/backend/api/views.py` | 所有 API 接口（上传课程、查询任务状态、获取结果、生成 AI 建议） |
| `web/backend/api/tasks.py` | 异步任务流水线（转码 → 骨架提取 → 行为识别 → 结果汇总） |
| `web/backend/templates/main.html` | 前端数据面板（ECharts 可视化 + 视频播放 + 签到表） |

**操作流程：**

1. 点击右下角"上传课程视频"按钮，弹出表单
2. 填写课程名称、简介，上传学生名单 CSV/Excel 表格，选择监控视频文件
3. 点击"上传并处理"，后台自动启动异步任务：
   - **阶段 1 — 视频转码**：统一编码格式，确保后续处理兼容
   - **阶段 2 — 骨架提取**：调用 `behavior_classification/get_pose.py`，逐帧运行 YOLOv11-Pose 提取 17 个关键点
   - **阶段 3 — 行为识别**：调用 `behavior_classification/predict.py`，ST-GCN 对骨架序列分类，输出 7 类行为标签
   - **阶段 4 — 人脸识别**：调用 `face_recognition/video_face_recognition.py`，ArcFace 人脸匹配，关联学生身份
   - **阶段 5 — 结果汇总**：统计各行为帧数、计算出勤率、生成 JSON 结果
4. 处理完成后，数据面板自动刷新，展示：

**数据面板包含的图表与信息（`templates/main.html`）：**

- **课堂总体概览**（左侧顶部）：出勤率、专注度指数（加权评分）、不良行为比例、举手次数
- **行为分布柱状图**：7 类行为各自的出现帧数统计
- **行为趋势分析**：堆叠面积图，展示各行为随时间的变化趋势
- **课程信息**（中间顶部）：课程名称、总人数、课程简介
- **课堂监控画面**：处理后的标注视频播放器，画面叠加骨架和行为标签
- **未出勤人员**：缺勤/请假学生名单（从学生名单对比人脸签到结果得出）
- **签到表**：所有学生的出勤状态汇总
- **课堂互动分析**（右侧）：举手 + 站立行为的时间趋势折线图
- **课堂纪律分析**：不良行为（传递物品、东张西望、睡觉、低头）饼图占比
- **AI 教学建议**：调用 DeepSeek 大模型，根据统计数据生成结构化的课堂评价 + 亮点 + 改进建议

---

#### 功能 5：模型训练流水线

**核心文件**：`behavior_classification/train.py`、`behavior_classification/data_loader.py`、`behavior_classification/data_augmentation.py`

虽然不是最终用户直接使用的功能，但这是整个行为识别能力的基础。训练流程为：

1. 从 19 类原始动作视频中，用 YOLOv11-Pose 逐帧提取骨架坐标，保存为 CSV
2. 通过 `data_augmentation.py` 做骨架级数据增强（随机旋转、平移、缩放），生成增强数据集
3. `data_loader.py` 加载骨架序列，构造时空图输入
4. `train.py` 训练 ST-GCN：交叉熵损失 + Adam 优化器 + ReduceLROnPlateau 调度 + 100 epoch

模型文件：`behavior_classification/net/st_gcn.py`（ST-GCN 主体，8104 行）、`behavior_classification/net/utils/graph.py`（人体骨架图结构）、`behavior_classification/net/utils/tgcn.py`（时空图卷积单元）

---

### 三、技术架构总结（约 1 分钟）

整体采用 **三阶段流水线**：

```
视频帧 → YOLOv11-Pose (get_pose.py) → 17 个关键点骨架坐标
                ↓
    骨架序列 → ST-GCN (predict.py) → 19 类基础动作 → 映射为 7 类课堂行为
                ↓
    人脸区域 → ArcFace (face_detector.py) → 512 维特征向量 → 余弦匹配 → 身份确认
                ↓
    汇总统计 → Django API (views.py + tasks.py) → ECharts 面板 + DeepSeek AI 建议
```

**7 类课堂行为**（19→7 映射）：

| 课堂行为 | 包含的基础动作编号 |
|---|---|
| 听讲 | 0, 1, 2, 8 |
| 举手 | 3, 4, 5, 6 |
| 站立 | 7 |
| 传递物品 | 9, 10, 11, 12 |
| 东张西望 | 13, 14, 15, 16 |
| 睡觉 | 17 |
| 低头 | 18 |

**关键文件速查表：**

| 文件 | 功能 |
|---|---|
| `behavior_classification/real_time_detection.py` | 实时摄像头：行为识别 + 人脸识别 + 骨架可视化 |
| `behavior_classification/get_pose.py` | YOLOv11-Pose 姿态提取接口 |
| `behavior_classification/predict.py` | ST-GCN 行为分类推理接口 |
| `behavior_classification/train.py` | ST-GCN 模型训练脚本 |
| `behavior_classification/data_augmentation.py` | 骨架数据增强 |
| `behavior_classification/net/st_gcn.py` | ST-GCN 网络模型主体 |
| `face_recognition/video_face_recognition.py` | 视频/摄像头人脸识别 |
| `face_recognition/face_registration_tool.py` | 人脸注册录入工具（摄像头/图片/文件夹三种模式） |
| `face_recognition/face_detector.py` | ArcFace 人脸检测 + 特征提取底层封装 |
| `face_recognition/face_database.py` | 本地人脸库管理（pkl + JSON 持久化） |
| `web/backend/api/views.py` | Web API 接口（上传、状态查询、结果获取、AI 建议） |
| `web/backend/api/tasks.py` | 异步任务流水线（转码→骨架→行为→人脸→汇总） |
| `web/backend/templates/main.html` | ECharts 数据可视化面板 + 视频播放 + 签到表 |

---

### 四、总结与展望（约 30 秒）

**贡献：** 多模态融合（姿态 + 行为 + 人脸）→ 端到端自动化 → Web 可视化交付 → AI 建议闭环。

**改进方向：** 行为类别扩展、遮挡鲁棒性提升、边缘设备部署、结合语音多模态分析。

我的汇报到此结束，请各位老师批评指正，谢谢！

---

## 第二部分：答辩问答环节

---

### Q1：为什么选择 ST-GCN 而不是其他行为识别方法？（高频必问）

行为识别主要有两条路线：基于 RGB 视频的 3D 卷积（I3D、SlowFast）和基于骨架的图卷积（ST-GCN）。

我选 ST-GCN 的理由：**计算效率**——骨架坐标数据量远小于原始 RGB 像素，17 个点的坐标序列对 GPU 几乎无压力；**噪声鲁棒性**——骨架对光照、服装变化不敏感；**建模自然**——人体骨架本身就是一张图，图卷积天然适合捕获关节间的空间关系 + 帧间的时序关系。

---

### Q2：19 类基础动作和 7 类课堂行为是什么关系？

19 类是 ST-GCN 训练时的细粒度动作类别（来自训练数据集划分），7 类是从课堂教学语义出发做的聚合。比如"举手"包含了 4 个不同幅度/角度的细分动作（编号 3-6），但在课堂场景下它们都统一为"举手回答问题"。映射关系定义在 `real_time_detection.py` 的 `action_mapping` 字典中（第 33-41 行）。

---

### Q3：人脸注册的三种模式分别怎么用？

| 模式 | 命令 | 场景 |
|---|---|---|
| 摄像头 | `--mode camera --name "张三"` | 学生现场面对摄像头，按空格键采 5 张 |
| 图片 | `--mode image --name "张三" --input photo.jpg` | 单张照片注册 |
| 文件夹 | `--mode folder --name "张三" --input ./photos/` | 批量导入多张照片 |

采集的特征向量保存在 `face_database/face_features.pkl`，人员元数据在 `face_database/metadata.json`，人脸原图在 `face_database/face_images/`。

---

### Q4：ArcFace 相比 FaceNet 有什么优势？

ArcFace 的核心是 Additive Angular Margin 损失——在角度空间中加一个 margin，让不同人的特征向量在超球面上拉开更大角度距离，同一人的特征更聚集。相比 FaceNet 的 Triplet Loss，ArcFace 不需要精心挑选三元组，训练更稳定，且角度 margin 具有更清晰的几何可解释性。

---

### Q5：系统能同时处理多少人？

YOLOv11-Pose 一帧内同时检测所有人，不随人数线性增长。ST-GCN 对每个检测到的人单独推理，输入仅 17×2 骨架坐标，非常轻量。瓶颈在 YOLO 前向推理，在 RTX 3060 上单帧约 30-50ms（20-30 FPS），标准 40-50 人教室可以稳定运行。

---

### Q6：如何处理遮挡问题？

两个层面：**姿态估计层**——YOLOv11-Pose 对部分遮挡有一定鲁棒性，可从可见关节推测被遮挡关节；**行为识别层**——50 帧滑动窗口 + 0.6 置信度阈值，短暂遮挡由窗口平滑，低置信度结果直接丢弃。严重遮挡（完全被前排挡住）是当前局限，未来可引入多摄像头视角融合。

---

### Q7：DeepSeek AI 建议怎么生成的？

`web/backend/api/views.py` 第 60-80 行定义了提示词模板。分析完成后，系统将出勤率、专注度指数、各行为占比等统计数据填入结构化提示词，调用 DeepSeek API 生成固定模板的输出：课堂整体评价 → 亮点 → 需改进方面 → 教学建议。

---

### Q8：实时检测中的"预测平滑"是怎么做的？

代码在 `real_time_detection.py` 的 `smooth_predictions` 方法（第 157-201 行）。核心逻辑：缓存最近 5 帧的预测结果 → 统计各行为类别的出现次数和平均置信度 → 多数投票选出最频繁类别 → 需要连续 2 帧一致或置信度 > 0.8 才输出，否则保持上一个稳定结果。这样避免了单帧误检导致行为标签闪烁。

---

### Q9：Web 平台的异步任务是怎么设计的？

`web/backend/api/tasks.py` 定义了完整的 5 阶段流水线：视频转码（统一编码）→ 骨架提取（调用 `get_pose.py`）→ 行为识别（调用 `predict.py`）→ 人脸识别（调用 `video_face_recognition.py`）→ 结果汇总。每个阶段独立运行、异常可追溯，任务状态通过 `jobs.json` 持久化，前端每 1.5 秒轮询一次状态。

---

### Q10：训练数据怎么来的？小数据集会不会过拟合？

训练数据：19 类动作演示视频 → YOLOv11-Pose 逐帧提取骨架 → 保存为 CSV。缓解过拟合的措施：**骨架级数据增强**（`data_augmentation.py`，随机旋转 ±15°、平移 ±5%、缩放 ±10%）+ ReduceLROnPlateau 学习率调度，验证损失不降时自动降学习率。

---

### Q11：系统最大的困难是什么？怎么解决的？

**三模块集成联调**。YOLOv11-Pose（ONNX Runtime）、ST-GCN（PyTorch）、ArcFace（ONNX Runtime）使用不同推理框架、不同输入输出格式。解决方案：在 `real_time_detection.py` 中统一用滑动窗口 + 预测平滑串联各模块，Web 端将任务拆为独立阶段，每阶段异常可追溯。

---

### Q12：你做的和别人做的有什么不同？

1. **多模态融合的工程完整性**：不是单独的"行为检测"或"人脸识别"，而是"谁 + 做了什么"的一体化记录
2. **端到端自动化**：上传视频 → 全自动处理 → Web 面板 + AI 建议，零人工干预
3. **三个入口覆盖不同场景**：命令行摄像头实时检测 / 命令行人脸注册管理 / Web 平台离线分析，形成完整工具链

---

## 附录：答辩注意事项

1. **时间控制**：介绍 6-8 分钟，技术细节可依据老师兴趣展开或跳过
2. **讲 PPT 时指着图讲**，不要空说。准备一段录屏演示备用，防止现场 demo 翻车
3. **7 类行为**：听讲、举手、站立、睡觉、低头、东张西望、传递物品——烂熟于心
4. **态度**：先肯定老师问题再作答，不会的诚实说"这方面考虑不够深入，下来认真研究"
5. **文件对应关系**建议记住几个核心文件的路径，老师如果问"这个功能在哪里实现的"可以直接指出来
